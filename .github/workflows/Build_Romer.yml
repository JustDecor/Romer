name: Build and Deploy Romer

on:
  push:
    branches:
      - Romer # або main, якщо перейменуєш

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Replace API_KEY in index.html
        run: |
          sed -i 's/\${API_KEY}/${{ secrets.FIREBASE_API_KEY }}/g' index.html

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4.7.3
        with:
          branch: gh-pages       # Гілка, куди пушиться деплой
          folder: .              # Каталог, який деплоїш
          token: ${{ secrets.GITHUB_TOKEN }}
